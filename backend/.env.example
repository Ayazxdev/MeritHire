DATABASE_URL=postgresql+asyncpg://postgres:1234@localhost:5432/fairhire

# Agent service base URLs (your teammates will run these)
SKILL_AGENT_URL=http://127.0.0.1:8001
BIAS_AGENT_URL=http://127.0.0.1:8002
MATCH_AGENT_URL=http://127.0.0.1:8003


# Server signing keys (for Skill Passport)
# You can generate these using the command: python -c "from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey; from cryptography.hazmat.primitives import serialization; import base64; k=Ed25519PrivateKey.generate(); pk=k.public_key(); print('PRIVATE=', base64.b64encode(k.private_bytes(serialization.Encoding.Raw, serialization.PrivateFormat.Raw, serialization.NoEncryption())).decode()); print('PUBLIC=', base64.b64encode(pk.public_bytes(serialization.Encoding.Raw, serialization.PublicFormat.Raw)).decode())"

SIGNING_PRIVATE_KEY_B64=
SIGNING_PUBLIC_KEY_B64=

APP_SECRET=dev-secret
